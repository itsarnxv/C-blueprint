/*Write a program that:
Declares an integer variable x and assigns it a value.
Prints the value of x.
Uses comments to explain what each line is doing.*/

#include<stdio.h>
int main()
{
    //  Declare an integer variable named x and assign it the value 25
    int x = 100 ; 

    // Print the value stored in variable x to the screen
    printf("The value of x is: %d\n", x);

    return 0;

}